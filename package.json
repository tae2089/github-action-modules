{
  "name": "github-action-modules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:release-noti": "ncc build src/release-noti.js -o dist/release-noti",
    "build:generate-tag": "ncc build src/generate-tag.js -o dist/generate-tag"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.6.8"
  }
}
